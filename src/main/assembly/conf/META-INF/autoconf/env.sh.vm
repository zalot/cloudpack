#!/bin/bash
#set ($app_port = $numberUtil.createInteger(${replication_appserver_port}) )
#set ($jmx_port = $app_port + 9 )
#set ($debug_port = $app_port + 8 )
PRODUCTION=${replication_production}
JAVA_HOME=${replication_javahome}
OUTPUT_HOME=${replication_output}
JMX_PORT=$jmx_port
DEBUG_PORT=$debug_port

export PRODUCTION JAVA_HOME OUTPUT_HOME DEBUG_PORT JMX_PORT
export LANG=zh_CN.UTF-8